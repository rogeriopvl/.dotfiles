# this file loads specific linux config using

# it's disabled by default so you need to copy it to `linux.config` so that it
# can be loaded by the main config file. This copy will be ignored by git, so
# any permanent changes should be made also on the original file.

font-family = "JetBrainsMono NF"
font-size = 9
