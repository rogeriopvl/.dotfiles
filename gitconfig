[color]
    diff = auto
    status = auto
    branch = auto
[user]
    name = rogeriopvl
    email = rogerio@hey.com
    signingkey = /Users/rogeriopvl/.ssh/id_ed25519
[core]
    excludesfile = /Users/rogeriopvl/.gitignore_global
    editor = /opt/homebrew/bin/nvim
[merge]
    tool = vimdiff
    conflictstyle = diff3
[mergetool]
    prompt = false
    path = vim
[alias]
    graph = log --graph --all --decorate --stat --date=iso
    plog = log HEAD --pretty=format:%s
    st = status -sb
    rrh = pull --rebase origin HEAD
    prh = push origin HEAD
    pullrom = pull --rebase origin master
    scores = shortlog -sn --all --no-merges
    recap = log --all --oneline --no-merges --author=rogerio@hey.com
    today = log --since=00:00:00 --all --no-merges --oneline --author=rogerio@hey.com
    ddiff = -c diff.external=difft diff
[init]
    templatedir = ~/.dotfiles/git-templates
[github]
	user = rogeriopvl
[commit]
  gpgsign = true
[gpg]
  format = ssh
[gpg "ssh"]
  allowedSignersFile = /Users/rogeriopvl/.ssh/allowed_signers
