[color]
    diff = auto
    status = auto
    branch = auto
[user]
    name = rogeriopvl
    email = rogeriopvl@gmail.com
[core]
    excludesfile = /Users/rogeriopvl/.gitignore_global
    editor = /usr/local/bin/nvim
[merge]
    tool = vimdiff
    conflictstyle = diff3
[mergetool]
    prompt = false
    path = vim
[alias]
    graph = log --graph --all --decorate --stat --date=iso
    plog = log HEAD --pretty=format:%s
    st = status -sb
    rrh = pull --rebase origin HEAD
    prh = push origin HEAD
    pullrom = pull --rebase origin master
    scores = shortlog -sn --all --no-merges
    recap = log --all --oneline --no-merges --author=rogeriopvl@gmail.com
    today = log --since=00:00:00 --all --no-merges --oneline --author=rogeriopvl@gmail.com
[init]
    templatedir = ~/.dotfiles/git-templates
